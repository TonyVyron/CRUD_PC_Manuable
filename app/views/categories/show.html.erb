<div class="container">
  <div class="row">
    <div class="col-md-8">
      <div class="card mb-3 h-100">
        <div class="card-header">
          <h1 class="card-title">Category</h1>
        </div>
        <div class="card-body">
          <h2 class="card-subtitle mb-2 text-muted"> <%= @category.name %></h2>
          <%= link_to 'Back to Categories', categories_path, class: 'btn btn-secondary mt-3' %>
        </div>
      </div>
    </div>
  </div>
</div>
