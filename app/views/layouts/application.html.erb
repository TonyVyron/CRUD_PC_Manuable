<!DOCTYPE html>
<html>
  <head>
    <title>StoreProducts</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container">
      <ul class="nav nav-tabs mb-3" id="pills-tab" role="tablist">
          <li class="nav-item" role="presentation">
            <a class="nav-link <%= 'active' if request.path.start_with?(products_path) %>" id="pills-products-tab" href="<%= products_path %>" role="tab" aria-controls="pills-products" aria-selected="<%= 'true' if request.path.start_with?(products_path) %>">Products</a>
          </li>

          <li class="nav-item" role="presentation">
            <a class="nav-link <%= 'active' if request.path.start_with?(categories_path) %>" id="pills-categories-tab" href="<%= categories_path %>" role="tab" aria-controls="pills-categories" aria-selected="<%= 'false' if request.path.start_with?(categories_path) %>">Categories</a>
          </li>
        </ul>
    </div>
    <%= yield %>
  </body>
</html>
